[phases.setup]
nixPkgs = ["nodePackages.npm", "python3", "pkg-config", "pixman", "cairo", "pango", "libjpeg", "libpng", "giflib", "librsvg", "chromium"]

[phases.build]
cmds = ["npm ci"]

[start]
cmd = "PUPPETEER_EXECUTABLE_PATH=$(which chromium) node index.js"
